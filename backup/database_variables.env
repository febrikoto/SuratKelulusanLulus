# Variabel koneksi database
# Gunakan file ini untuk mengatur koneksi database pada server baru
# Copy file ini ke .env pada server baru

# URL koneksi lengkap
DATABASE_URL=${DATABASE_URL}

# Komponen koneksi individual
PGHOST=${PGHOST}
PGPORT=${PGPORT}
PGUSER=${PGUSER}
PGPASSWORD=${PGPASSWORD}
PGDATABASE=${PGDATABASE}

# Variabel aplikasi
NODE_ENV=production
SESSION_SECRET=gunakan_string_random_yang_panjang_dan_aman_disini